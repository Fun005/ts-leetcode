/**
 * 两数之和(时间复杂度0(N^2))
 * @param { number[] } nums 
 * @param { number } target 
 * @returns { number[] | undefined }
 */
export function twoSum1(nums: number[], target: number): number | undefined {
    return []
}